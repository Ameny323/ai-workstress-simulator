DATABASE_URL=postgresql://user:password@localhost:5432/workstress_db
JWT_SECRET=
CORS_ORIGINS=http://localhost:5173

# OpenAI (ARIA adaptive manager) -- server-side only. Never commit a real
# key here, in source, in the frontend, or in any WebSocket payload/log.
OPENAI_API_KEY=
OPENAI_MODEL=
